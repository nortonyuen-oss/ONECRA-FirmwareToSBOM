{
  "pack": "vendor-st",
  "description": "STMicroelectronics STM32 SDKs.",
  "signatures": [
    {
      "name": "stm32-hal",
      "supplier": "STMicroelectronics",
      "type": "library",
      "purl": "pkg:generic/stm32-hal",
      "description": "STM32 HAL/LL drivers",
      "patterns": [
        {
          "regex": "STM32[A-Z][0-9]xx? HAL",
          "weight": 0.8
        },
        {
          "regex": "HAL_(?:Init|GPIO|UART|RCC)_?[A-Za-z]*",
          "weight": 0.5
        },
        {
          "regex": "stm32[a-z][0-9]+xx",
          "weight": 0.4
        }
      ],
      "version_note": "The STM32 HAL exposes its version as numeric macros (__STM32xxxx_HAL_VERSION_MAIN/SUB1/SUB2), which compile to immediates rather than to a string. Obtain the version from the STM32Cube package used for the build."
    }
  ]
}
